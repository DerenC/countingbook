# name
name: new_release

# trigger
on:
  release:
    types: [published]

# one or more jobs
jobs:
    new_release:
        runs-on: ubuntu-latest
        steps:
            - id: s01
              run: echo "There is a new release"